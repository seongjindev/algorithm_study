너비우선탐색
    최단경로
    q1. 정점(node)A에서 정점(node)B로 가는 경로가 존재하는가?
    q2. 정점(node)A에서 정점(node)B로 가는 최단 경로는 무엇인가?

    큐(queue)를 사용하여 인접한 노드를 하나씩 확인하면서 최단경로를 확인 할 수 있다.
    다녀간 노드는 따로 저장해놓고, 무한 루프에 빠지지 않게 해야한다.